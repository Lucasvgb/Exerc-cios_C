#include <stdio.h> //biblioteca padrão do C, STD = standard e o IO = input e output//

int main() //metodo principal onde o programa começa//
{
    printf("Hello world!"); // pritf = imprimir formatado//
    return 0; //padrão//
}
